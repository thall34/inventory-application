<html>
    <head>
        <title><%= title %></title>
        <link rel="stylesheet" href="/app.css">
        <link rel="stylesheet" href="/index.css">
        <link rel="stylesheet" href="/park.css">
    </head>
    <body>
        <%- include('./partials/header.ejs', { title: title }) %>
        <main>
            <div class="park-card more-info"> 
                <h2><%= data.name %></h2>
                <h3>Coasters (<%= data.coasters.length %>):</h3>
                <% if (data.coasters.length > 0) { %>
                    <ul class="coaster-list">
                        <% data.coasters.forEach((coaster) => { %>
                            <li><%= coaster %></li>
                        <% }) %>
                    </ul>
            <% } else { %>
                <p>No coasters found</p>
            <% } %>
            </div>
            <div class="park-buttons">
            <a href="/park/update/<%= data.park_id %>">
                <button>Update Park</button>
            </a>
            <a href="/park">
                <button>Back to park list</button>
            </a>
            </div>
        </main>
        <%- include('./partials/footer.ejs') %>
    </body>
</html>